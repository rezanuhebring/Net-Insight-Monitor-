# .gitignore for the Net-Insight Monitor Project

# -----------------------------------------------------------------------------
# -- Files and Directories Generated by the Server Setup Script
# -----------------------------------------------------------------------------
# These files are created by setup.sh based on user input, so they should not be in version control.
# The repository should only contain the setup script itself.
central_server_package/Dockerfile
central_server_package/docker-compose.yml
central_server_package/nginx/


# -----------------------------------------------------------------------------
# -- Agent Configuration and Runtime Files
# -----------------------------------------------------------------------------
# IGNORE the actual agent config files that contain secrets like the API key.
# ALWAYS keep the ".template" files in the repository.
*agent_config.env
*agent_config.ps1

# IGNORE agent runtime files, like logs and lock files.
*.log
*.lock


# -----------------------------------------------------------------------------
# -- General Secrets and Environment Files
# -----------------------------------------------------------------------------
# A general rule to prevent any .env files from being committed.
*.env
.env.*
!.env.example


# -----------------------------------------------------------------------------
# -- Databases and Cache
# -----------------------------------------------------------------------------
# Ignore any local SQLite database files created during testing.
*.sqlite
*.sqlite3
*.db
*.db-journal


# -----------------------------------------------------------------------------
# -- Common IDE and Editor Directories
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo


# -----------------------------------------------------------------------------
# -- OS-specific generated files
# -----------------------------------------------------------------------------
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
*.stackdump


# -----------------------------------------------------------------------------
# -- General Build and Dependency Artifacts
# -----------------------------------------------------------------------------
# While not used yet, this is good practice for future-proofing.
node_modules/
dist/
build/