# agent_config.env - Configuration for the Net-Insight Monitor Linux Agent
# This file is sourced by the main monitoring script. Use bash variable syntax.
# Example: VARIABLE_NAME="value" or ARRAY_VAR=("value1" "value2")

# --- CRITICAL CONFIGURATION ---
# IMPORTANT: These three values MUST be configured correctly for the agent to work.

# The unique identifier for this agent. This should be a descriptive, URL-safe string.
# Example: AGENT_IDENTIFIER="Main-Office-ISP-Fiber"
AGENT_IDENTIFIER="<REPLACE_WITH_UNIQUE_ID>"

# The full URL to the submission API on your central Net-Insight Monitor server.
# Example for Production: CENTRAL_API_URL="https://monitor.yourcompany.com/api/submit_metrics.php"
# Example for Development: CENTRAL_API_URL="http://192.168.1.100:8080/api/submit_metrics.php"
CENTRAL_API_URL="<REPLACE_WITH_YOUR_SERVER_URL>"

# The secret API key for this agent. This key must be generated on the central server
# and assigned to a profile that matches the AGENT_IDENTIFIER above.
CENTRAL_API_KEY="<PASTE_AGENT_API_KEY_HERE>"


# --- GENERAL CONFIGURATION ---

# The type of agent. This helps categorize agents on the dashboard.
# Recommended values: "ISP" or "Client".
AGENT_TYPE="ISP"

# The specific network interface to bind tests to (e.g., "eth0" or "wlan0").
# Leave blank to let the system choose the default route.
# This is useful for multi-homed servers or for forcing tests over a specific connection.
NETWORK_INTERFACE_TO_MONITOR=""


# --- TEST-SPECIFIC CONFIGURATION ---

# A space-separated list of reliable hosts to ping for connectivity and latency checks.
# It is recommended to use a mix of IP addresses and domain names.
PING_HOSTS=("8.8.8.8" "1.1.1.1" "google.com")
PING_COUNT=10

# The host to use for DNS resolution checks.
DNS_CHECK_HOST="www.google.com"

# The URL to use for HTTP availability and response time checks.
HTTP_CHECK_URL="https://www.google.com"

# Additional arguments for the speedtest command.
# For example, to select a specific server: SPEEDTEST_ARGS="--server-id=1234"
# The script will automatically add --source or --interface if NETWORK_INTERFACE_TO_MONITOR is set.
# The license acceptance flags are recommended to avoid interactive prompts.
SPEEDTEST_ARGS="--accept-license --accept-gdpr"


# --- FEATURE FLAGS ---
# Set any of these to "false" to disable the respective test for this agent.

ENABLE_PING=true
ENABLE_DNS=true
ENABLE_HTTP=true
ENABLE_SPEEDTEST=true
ENABLE_WIFI=true